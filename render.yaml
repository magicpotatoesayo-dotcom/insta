services:
  - type: web
    name: instagram-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python hi2Instagram.py
    plan: free
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
